import { HoverHoverDirective } from './hover-hover.directive';

describe('HoverHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverHoverDirective();
    expect(directive).toBeTruthy();
  });
});
